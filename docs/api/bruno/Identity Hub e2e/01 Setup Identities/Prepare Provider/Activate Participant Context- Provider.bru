meta {
  name: Activate Participant Context- Provider
  type: http
  seq: 2
}

post {
  url: {{PROVIDER_IDHUB_ID_API}}/v1alpha/participants/{{PROV_ID}}/state?isActive=true
  body: none
  auth: inherit
}

params:query {
  isActive: true
}

headers {
  x-api-key: {{PROVIDER_IH_APIKEY}}
}

settings {
  encodeUrl: true
  timeout: 0
}
