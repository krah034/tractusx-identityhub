meta {
  name: Get Provider VC
  type: http
  seq: 6
}

get {
  url: {{PROVIDER_IDHUB_ID_API}}/v1alpha/participants/{{PROV_ID}}/credentials
  body: json
  auth: none
}

headers {
  x-api-key: {{PROVIDER_IH_APIKEY}}
}

script:pre-request {
  
  req.setHeader("x-api-key", bru.getEnvVar("PROVIDER_IH_APIKEY"));
}

settings {
  encodeUrl: true
  timeout: 0
}
