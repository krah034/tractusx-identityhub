meta {
  name: Activate Participant Context
  type: http
  seq: 2
}

post {
  url: {{ISSUER_ID_API}}/v1alpha/participants/{{ISS_ID}}/state?isActive=true
  body: none
  auth: inherit
}

params:query {
  isActive: true
}

headers {
  x-api-key: {{ISSUER_APIKEY}}
}

settings {
  encodeUrl: true
  timeout: 0
}
