meta {
  name: Add Consumer Holder
  type: http
  seq: 7
}

post {
  url: {{ISSUER_ISS_API}}/v1alpha/participants/{{ISS_ID}}/holders
  body: json
  auth: inherit
}

headers {
  x-api-key: {{ISSUER_APIKEY}}
}

body:json {
  {
    "holderId" : "{{CONS_ID}}", 
    "did" : "{{CONS_ID}}", 
    "name" : "{{CONS_ID}}"
  }
}

script:pre-request {
  
}

settings {
  encodeUrl: true
  timeout: 0
}
